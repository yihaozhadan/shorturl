# Backend API Configuration
# This value is used server-side only and will NOT be exposed to the browser
BACKEND_URL=http://localhost:8080

# Public Backend URL for Short Links
# This is the public-facing URL where short URLs will redirect (can be exposed to browser)
PUBLIC_BACKEND_URL=http://localhost:8080
